# What is this?

Listen, imagine .bar/ was equivalent to your .config/ in your homedir.

This is just a test to see behavior:

- What if .config/ already exists (it likely does)?
- Will this clobber stuff?
- For testing, ensure you have a .bar/ directory in your homedir before you `stow bar`
   just to see what happens.
